#include<iostream>
int vol(int);
double vol(double,int);
double vol(longint,int,int);
    int main(){
        cout<<"vol is"<<endl;
        cout<<"vol is "<<endl;
    }
int vol(int s)
{ return (s*s*s);


double vol(double r,int h)
{return(3.14*r*r*h)
}

    